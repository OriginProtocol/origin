matrix:
  include:
  - language: node_js
    node_js: lts/*
    before_script:
    - cd origin-dapp && npm install
    script:
    - npm test

  # FIXME: remove dependency between origin-faucet and origin-js/contracts/test-alt
  - language: node_js
    node_js: lts/*
    before_script:
    - cd origin-faucet && npm install && cd ../origin-js && npm install
    script:
    - npm test

  - language: python
    python: 3.6
    before_script:
    - cd origin-bridge
    script:
    - pytest --flakes --codestyle
