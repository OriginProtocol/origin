{
  "name": "@origin/ip2geo",
  "version": "0.1.0",
  "description": "Origin IP to geo",
  "license": "MIT",
  "main": "src/index.js",
  "scripts": {
    "test": "NODE_ENV=test mocha --timeout 10000 --exit",
    "lint": "eslint . && npm run prettier:check",
    "prettier": "prettier --write *.js \"src/**/*.js\"",
    "prettier:check": "prettier -c *.js \"src/**/*.js\""
  },
  "dependencies": {
    "geoip-lite": "^1.3.6",
    "node-fetch": "^2.3.0"
  },
  "devDependencies": {
    "eslint": "^5.15.3",
    "mocha": "^5.2.0",
    "prettier": "^1.16.4"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
