describe('initial state', () => {

  it ('should have 5 listings', () => {
  })

  it('should have 5 listings in postgresql', () => {
  })

  it('should return 5 listings from discovery', () => {
  })

})
