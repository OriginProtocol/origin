FROM python:3.6

COPY ./origin-bridge /app
WORKDIR /app

RUN pip install -r requirements.txt
RUN pip install gunicorn

EXPOSE 5000

CMD [ \
    "gunicorn", \
    "-b", ":5000", \
    "--access-logfile", "-", \
    "--error-logfile", "-", \
    "main:app" \
]
