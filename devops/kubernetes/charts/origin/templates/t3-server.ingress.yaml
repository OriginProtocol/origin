{{- if ne .Release.Namespace "prod" -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "t3server.fullname" . }}
  labels:
    app: {{ template "t3server.fullname" . }}
    app.kubernetes.io/name: origin
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/version: {{ .Chart.AppVersion }}
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: origin-t3server
  annotations:
    kubernetes.io/ingress.class: {{ .Release.Namespace }}-ingress
    kubernetes.io/tls-acme: "true"
    certmanager.k8s.io/cluster-issuer: {{ .Values.clusterIssuer }}
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-methods: "GET, POST"
    nginx.ingress.kubernetes.io/cors-allow-origin: "*"
    nginx.ingress.kubernetes.io/limit-rps: "50"
spec:
  tls:
    - secretName: {{ template "t3server.host" . }}
      hosts:
        - {{ template "t3server.host" . }}
  rules:
  - host: {{ template "t3server.host" . }}
    http:
      paths:
        - path: /
          backend:
            serviceName: {{ template "t3server.fullname" . }}
            servicePort: 5000
{{- end }}
